import SluglineElement from "./base";

export default class SluglineActionElement extends SluglineElement {
    constructor() {
        super();
    }
}

customElements.define("slugline-action", SluglineActionElement);