import SluglineElement from "./base";

export default class SluglineTransitionElement extends SluglineElement {
    constructor() {
        super();
    }
}

customElements.define("slugline-transition", SluglineTransitionElement);