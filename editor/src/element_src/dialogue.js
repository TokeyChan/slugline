import SluglineElement from "./base";

export default class SluglineDialogueElement extends SluglineElement {
    constructor() {
        super();
    }
}

customElements.define("slugline-dialogue", SluglineDialogueElement);